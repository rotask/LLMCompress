# LLMCompress runtime dependencies.
# Pinned to compatible-release ranges that match the thesis environment (2024).
# For full-precision GPT-2 / CPU smoke tests, bitsandbytes is optional.

# Core ML
torch>=2.1,<3.0
transformers>=4.40,<5.0
accelerate>=0.27         # required by transformers' device_map="auto"

# Quantization (only needed for non-GPT-2 models on CUDA)
bitsandbytes>=0.43; sys_platform == "linux"

# Arithmetic-coding backend (used by Arithmetic_Coder.py)
constriction>=0.3,<0.5

# Numerics & utilities
numpy>=1.24,<2.0         # bitsandbytes 0.43 not yet numpy-2 compatible
tqdm>=4.65

# Optional: Flash-Attention 2 — install only if you have CUDA + matching torch.
# Uncomment to install separately; the project falls back to default attention.
# flash-attn>=2.5
